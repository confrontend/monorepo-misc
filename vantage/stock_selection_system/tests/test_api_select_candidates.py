"""
Superseded by tests/test_api_fetch_candidates.py -- the
/api/actions/select-candidates endpoint was renamed to
/api/actions/fetch-candidates (SelectCandidatesRequest/
select_candidates_endpoint -> FetchCandidatesRequest/
fetch_candidates_endpoint) to match the current spec's exact endpoint name.
This file is kept as an empty placeholder (rather than deleted) because this
environment's filesystem mount doesn't allow deleting files in the synced
workspace copy; see tests/test_api_fetch_candidates.py for the real tests.
"""
