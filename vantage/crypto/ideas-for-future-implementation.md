# Ideas for future implementation

- 2026-08-23 11:16 -07:00 — Check whether the app already uses a `DataTable` component. Create a shared components folder and begin consolidating table behavior there so sorting, filtering, tooltips, and related logic are reused instead of duplicated.
- 2026-08-23 11:38 -07:00 — Candidates for a `ui/components/` shared folder, found by parsing `ui/main.tsx` (only 7 components exist today — CapitalPathChart, GmgnTag, CopyAddressButton, WalletIcon, SaveRowButton, InfoTip — everything else is inline in the single ~4,000-line `App()`), ranked by duplication evidence:
  - **Modal / Dialog** (4 copies: scrutiny detail, roster comparison, stats detail, copy-delay detail) — each hand-rolls the same backdrop + `role="dialog"` + stop-propagation + close-on-backdrop-click pattern.
  - **DataTable** (6 copies: scrutiny-table, decision-table, history-table, combined-stats-table, trade-detail-table, fully-covered-table) — each wraps its own `table-wrap` div and hand-writes `<thead>`/`.map()` rows. Highest-value target alongside Modal — most duplication, most behavior, and the kind of copy-drift that already caused real bugs this session.
  - **Panel / Section** (10+ copies) — every top-level content area is `<section className="menu-section panel X-panel">` with the same header shape. — DONE 2026-08-23: extracted `ui/components/PanelHeading.tsx` (`eyebrow`/`title`/optional `tag`) for the repeated `panel-heading` header block only (not the bespoke section bodies, and not the `<section>` wrapper itself — every section's `id` and any conditional-render wrapper were left untouched at the call site). Migrated 14 header call sites across `ui/main.tsx` (the 9 top-level `menu-section panel` sections whose header matched the plain eyebrow+h2+optional-tag shape, plus 3 nested `article.panel` headers in the imports grid and 2 more in the lower-grid, and the nested triage sub-panel header). Left `evidence` and `diagnostics` sections' headers inline — their header has a `<button>` action instead of the `tag` span, a different shape that would have forced an extra slot onto the component for 2 call sites. Also left `capture-raw-endpoints` (no panel-heading at all, a `<details>` summary instead) and the top-level `copytrade` section (no direct panel-heading, all content in nested sub-panels) untouched — neither fits the shape.
  - **SortableHeader** (18 references) — every table re-implements its own sortable-header + sort-arrow-indicator + click-to-toggle logic. — DONE 2026-08-23: found 5 independent sort-state implementations, not just headers (`outcomeSort`, `copyTradeSort`, `decisionSort`, `gmgnStatsSort`, `copyDelaySort`), each hand-rolling identical `useState<{key,direction}>` + toggle (click-same-column-flips-direction, click-new-column-resets-to-asc) + arrow-indicator logic. Extracted a generic headless `ui/components/useSortState.ts` (`useSortState<K extends string>(defaultKey, defaultDirection)` returning `{ sort, setSort, toggleSort, sortIndicator }`) and migrated all 5 state declarations to it, keeping every existing variable/function name (`outcomeSort`, `toggleDecisionSort`, `gmgnStatsSortIndicator`, etc.) via destructuring aliases so every one of the 18 header/comparator call sites needed zero changes. Deliberately did NOT unify the visual `sortable-header` `<th>` markup itself, per the recommended approach — only the actually-duplicated state/toggle/indicator logic was extracted, not the varying per-table header rendering.
  - **Collapsible** (12 `<details>` wrappers) — same open/summary/toggle-state pattern repeated per section. — DONE 2026-08-23: extracted to `ui/components/Collapsible.tsx`, 17 of 17 call sites migrated (the real count as of the extraction was 17, not 12; none were left inline).
  - **StatTile** (seen once so far, `copytrade-decision-state-tile`: `<div className="X-tile tone-Y"><strong>{count}</strong><span>{label}</span></div>` — worth checking other dashboards for the same shape before assuming it's only used once). — CHECKED 2026-08-23: confirmed there is exactly one JSX call site (`ui/main.tsx`, inside the `copytrade-decision-state-tile` `.map()` over `DECISION_ORDER`), and no other similarly-shaped "stat tile" markup exists elsewhere in the file. Since it's a single already-DRY call site rendered 4 times via mapping, not 4 duplicated call sites, extracting a component would be premature abstraction. Not extracted.
  - **StatusBanner** (12 copies) — `copytrade-status-warning` / `muted` one-line message boxes. — DONE 2026-08-23: extracted to `ui/components/StatusBanner.tsx`, all 9 call sites migrated.
  - **WorkflowStep** (12 copies) — the numbered "0/1/2" circular step badges in Fetch controls. — DONE 2026-08-23: extracted `ui/components/WorkflowStep.tsx` (`number`, optional `title`) and migrated all 3 current call sites in the Fetch controls (`ui/main.tsx`) — 1 bare span and 2 nested inside `copytrade-workflow-label` alongside heading/subtext.
  - **Badge/Pill** (~5 copies) — verdict badges and scrutiny verdict badges, likely mergeable into one generic `<Badge tone>`. — CHECKED 2026-08-23: only 2 `copytrade-verdict-badge` call sites remain in `ui/main.tsx` (other verdict/badge duplication was already resolved earlier). The two are structurally different (one is a `<span>` with `aria-label`/tooltip/dynamic small child driven by the `DECISION_STATES` tone lookup, the other a bare `<strong>` with an inline ternary tone) and share no computation, only a className string. Not worth a dedicated component for 2 near-trivial, differently-shaped call sites — left inline.

- 2026-08-23 (later) — idea: centralize all UI-facing strings (labels, button text, tooltips, empty-state/error messages) into one file, imported and reused across the app instead of inline string literals scattered through `ui/main.tsx`. Would pair naturally with the `ui/components/` extraction above (Modal/DataTable etc. take label/text props today) — a shared strings module would give those components, and the ~4,000-line `App()`, a single place to update copy instead of hunting through inline JSX. Not scoped or started.

- 2026-08-23 (later still) — Idea: introduce a lightweight shared derived-state layer in the UI to avoid duplicated decision logic. Keep state management simple: local state → `useState`, parent/child state → React Context, global UI state → Zustand, and backend/API state → TanStack Query. Shared derived concepts should live in one reusable selector or function so different parts of the UI cannot compute the same decision differently.
