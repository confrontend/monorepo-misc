{
  "project": "crypto",
  "version": "gmgn-v2-pre-event-only",
  "feature_source": "features",
  "feature_policy": "Only the explicit row.features object is eligible for discovery. Identifiers and point-in-time aggregates calculated strictly before each buy are allowed; outcomes, returns, delays, holding duration, fees, and post-event matching fields are rejected.",
  "allow_list": [
    {"name": "wallet_address", "justification": "Wallet identity is the predeclared CopyTrade independence group, not a return-based selection feature."},
    {"name": "token_symbol", "justification": "Token label is copied from the normalized GMGN round-trip record."},
    {"name": "signal_type", "justification": "Source-normalized signal type is stored at capture."},
    {"name": "token_address", "justification": "Token identity is part of the captured source record."},
    {"name": "chain", "justification": "Chain identity is part of the captured source record."},
    {"name": "prior_wallet_trade_count", "justification": "Count of stored wallet buy/sell rows strictly before the current buy event."},
    {"name": "prior_token_trade_count", "justification": "Count of stored token buy/sell rows strictly before the current buy event."},
    {"name": "prior_wallet_buy_volume_usd", "justification": "Sum of positive recorded buy cost_usd for this wallet strictly before the current buy event."}
  ],
  "reject_by_default": [
    "captured_at", "query_market_cap", "query_ath", "query_cur_data", "current_wallet_statistics",
    "post_event_rank", "later_roster_membership", "dune_outcome", "avg_holding_period",
    "hold_seconds", "wallet_return_percent", "entry_trade_amount_usd", "exit_trade_amount_usd",
    "edge_kept_percent", "entry_gap_seconds", "exit_gap_seconds", "gas_fee_usd", "outcome_at",
    "coverage_rate_percent", "coverage_status", "benchmark_return", "excess_return", "net_return_after_costs"
  ]
}
